import React, { memo } from 'react';
import { View, Text } from 'react-native';

function PostsApp() {
  return (
    <View>
      <Text>PostsApp</Text>
    </View>
  );
}

export default memo(PostsApp);
